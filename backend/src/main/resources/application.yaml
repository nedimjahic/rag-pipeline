server:
  port: 8081
spring:
  application:
    name: pipeline
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: llama3.2
        temperature: 0.7

logging:
  level:
    org.springframework.ai: DEBUG
    web: DEBUG